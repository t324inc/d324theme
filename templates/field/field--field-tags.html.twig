{#
/**
 * @file
 * Template for field_tags.
 */
#}
{% for item in items %}
  {% include '@d324theme/badge/badge.twig' with {
    type: 'light',
    content: item.content['#title'],
    url: item.content['#url'],
  } %}
{% endfor %}
