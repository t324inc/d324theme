{%
  set classes = [
  'paragraph',
  'paragraph--type--' ~ paragraph.bundle|clean_class,
  'paragraph--id--' ~ paragraph.id(),
  view_mode ? 'paragraph--view-mode--' ~ view_mode|clean_class,
  not paragraph.isPublished() ? 'paragraph--unpublished',
  'breakout-container',
  background ? 'b-lazy'
]
%}
{% block paragraph %}
  {% include '@d324theme/breakout_container/breakout_container.twig' with {
    fluid_container: paragraph.field_fluid_container.value,
    background: background,
    background_color: paragraph.field_background_color.value.0,
    background_color_2: paragraph.field_background_color_2.value.0,
    content: content,
    attributes: attributes.addClass(classes)
  } %}
{% endblock paragraph %}
